- title "Item"

%p
  %h3= @item.original_description

%br

- if @item.vendor_code
  %p
    %strong Vendor Code:
    = @item.vendor_code

- unless @item.prices.empty?
  %h4 Связанные элементы
  %table{:style => "width:100%"}
    %tr
      %th Складской код
      %th Наименование
      %th Цена
    - @item.prices.each do |price|
      = render 'prices/price', :price => price

  %strong
    = t('.pricehistory_title')
  #flot_placeholder{:style => "width:600px;height:300px;display:block;"}
  :javascript
    var historyData = [#{@item.get_history_data_for_javascript}];

%p
  = link_to "Edit", edit_item_path(@item)
  |
  = link_to "Destroy", @item, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to "View All", items_path
