- if @items
  %table
    %tr
      %th Наименование
      %th Прайсов

      - @items.each do |item|
        %tr
          %td{:id=>"item-#{item.id}", :class=>'found_item'}
            = item.original_description
          %td= item.prices.count
          %td= link_to 'Подробно', [item]