.accordion-group
  .accordion-heading
    %a.accordion-toggle{ :data => { :toggle => "collapse" }, :href => "#collpase-#{company.name}"}
      = company.name
  .accordion-body.collapse{ :id =>"collpase-#{company.name}", :style => "height:0px;", :data => {:url => "#{company.search_url}#{params[:q]}"}}
    .accordion-inner
