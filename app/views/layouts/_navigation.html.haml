.grid_12{:style=>"background: none repeat scroll 0 0 #666666; height:2.5em;"}
  .grid_6.alpha
    %ul.nav.main
      %li= link_to("Главная", root_path)
      %li= link_to("Поиск", searches_path)
      %li= link_to("Справочник", items_path)
      %li= link_to("Связи", links_path)

  .grid_3
    %ul.nav
      -if user_signed_in?
        %li
          %a Logged in as #{current_user.name}
        %li= link_to('Logout', signout_path)
      - else
        %li= link_to('Login', signin_path)

  .grid_3.omega.nav-search
    = search_box

.clear